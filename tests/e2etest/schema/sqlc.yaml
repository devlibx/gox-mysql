version: "2"
sql:
  - schema: "schema.sql"
    queries: "users.sql"
    engine: "mysql"
    gen:
      go:
        package: "users"
        out: "../sql/users"
        emit_interface: true
        emit_json_tags: true